DOMAIN=bitwarden.example.com

BITWARDEN_VERSION=1.37.1

#* Admin panel token — generate an argon2id hash with:
#*   docker run --rm -it vaultwarden/server:${BITWARDEN_VERSION} /vaultwarden hash
#* Keep the value in single quotes so the $ signs are not expanded. Empty = /admin disabled.
ADMIN_TOKEN=
